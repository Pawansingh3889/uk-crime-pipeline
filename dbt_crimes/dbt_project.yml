name: dbt_crimes
version: '1.0.0'
config-version: 2

profile: dbt_crimes

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_crimes:
    staging:
      +materialized: view
    marts:
      +materialized: table